node_modules/
data/*.db
data/*.db-wal
data/*.db-shm
.env
.DS_Store

# Downloaded cover art (run `npm run covers` to regenerate)
public/covers/
data/.steam-applist.json

# Per-deployment session signing key, generated at build time
.session-key
